spring:
  datasource:
    url: jdbc:h2:mem:retirementdb;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    defer-datasource-initialization: true   # wait for Hibernate, THEN run data.sql
    hibernate:
      ddl-auto: none        # schema.sql builds the tables, not Hibernate
    show-sql: true
  sql:
    init:
      mode: always          # run schema.sql then data.sql
  mail:
    host: ${MAIL_HOST:smtp.gmail.com}
    port: ${MAIL_PORT:587}
    username: ${MAIL_USERNAME:}
    password: ${MAIL_PASSWORD:}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
security:
  jwt:
    secret-key: ${JWT_SECRET_KEY:}
    expiration-time: 3600000
server:
  port: ${PORT:5000}