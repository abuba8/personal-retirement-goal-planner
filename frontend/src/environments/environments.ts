// src/environments/environments.ts
export const environment = {
  production: false,
  // change the port to match server.port (your .env PORT) if it isn't 8080
  baseApiUrl: "http://localhost:8081",
};
